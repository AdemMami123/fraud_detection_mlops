# Environment Configuration for Cloud Run

# API Configuration
PORT=8000
LOG_LEVEL=INFO
WORKERS=1

# Model Configuration
MODEL_PATH=models/rf_model.pkl
PARAMS_PATH=params.yaml

# CORS Settings (for frontend)
ALLOWED_ORIGINS=*

# Production Settings
ENVIRONMENT=production
DEBUG=false

# Optional: MLflow Tracking (if using external MLflow server)
# MLFLOW_TRACKING_URI=https://your-mlflow-server.com

# Optional: Google Cloud Storage (if storing models in GCS)
# GCS_BUCKET=your-bucket-name
# GCS_MODEL_PATH=models/rf_model.pkl
